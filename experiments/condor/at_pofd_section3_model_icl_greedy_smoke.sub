# HTCondor: SECTION-3 MODEL-SPECIFIC EQUILIBRIA -- 3-ROUND MISTRAL-7B SMOKE, GREEDY (main paper), 1 jobs.
# GENERATED by gen_pofd_sweep.py from the S3M block. Never edit by hand.
# Six checkpoints x seeds {0,42,43} at one matched surface: 18 production
# jobs. Existing older cells are intentionally not reused so every plotted
# value shares one code provenance.
# beta=W_PLAT=1, gamma=INNATE_LAMBDA=1 (inert at beta=1), FROZEN personal-history ICL
# (ICL_K=0, ICL_DAYS=8; no LoRA, no optimizer, no KL), Deffuant alpha=0.5 (production default), S=100 complete peer
# sweeps, both gates all_open, frozen weights, PARSE_MODE=strict, 3 rounds,
# MovieLens/Action 723. Only BASE_MODEL and replication seed change.
# This wave has NO lambda=0 arm. Perfect prediction is the common innate-mean
# reference and requires no GPU run.
# Gate: python experiments/scripts/cluster_pipelines/check_section3_model_icl.py --smoke --arm greedy
# Submit: bash experiments/condor/submit_pofd_sweep.sh <BID> section3_model_icl_greedy_smoke
universe          = vanilla
executable        = /home/gsmithline/perfsim/experiments/condor/run_one_pokec_gated_idempotent.sh
arguments         = $(tag) $(style) $(beta) $(seed) $(deploy_every) $(regime) $(pscale) $(anchor) $(pop) $(eps) $(gamma) $(wplat) $(mode) $(canary)

request_cpus      = 4
request_memory    = $(mem)
request_disk      = $(disk)
request_gpus      = 1
requirements      = (TARGET.CUDAGlobalMemoryMb >= 80000) && (TARGET.CUDADeviceName == "NVIDIA H100 80GB HBM3") && (TARGET.Machine != "g106.internal.cluster.is.localnet") && (TARGET.Machine != "i104.internal.cluster.is.localnet") && (TARGET.Machine != "i101.internal.cluster.is.localnet")

getenv            = False
environment       = "REPO=/home/gsmithline/perfsim CONDA_SH=/home/gsmithline/miniconda3/etc/profile.d/conda.sh ENV_NAME=opdyn WANDB_KEY_FILE=/home/gsmithline/.wandb_key WANDB_PROJECT=perfsim-gated-lm DATASET=movielens ML_TARGET=Action HF_HOME=/lustre/fast/fast/gsmithline/hf_cache HF_HUB_OFFLINE=1 AI_GATE_MODE=all_open PEER_GATE_MODE=all_open AI_GATE_REFERENCE=anchor EPS_AI=1 INNATE_LAMBDA=$(lam) AB_SWEEPS=$(sweeps) DEFFUANT_ALPHA=0.5 ICL_K=$(iclk) ICL_SNAPSHOT_ROUND=$(snap) ICL_DAYS=8 ICL_SELECT=random ICL_CTX_SOURCE=live USE_LORA=$(uselora) FRESH_EACH_ROUND=$(fresh) ANS_SAMPLE_K=$(ansk) ANS_SAMPLE_N=64 ANS_SAMPLE_T=1.0 LOG_GENDER_GAPS=$(gg) KL_DIRECTION=$(kldir) WITH_TWIN=1 SAVE_RAW_GEN=1 CHAT_THINKING=$(chatthink) BASE_MODEL=$(basemodel) TRAIN_CAP=723 N_ROUNDS=$(nrounds) EPOCH_SIZE=100 SFT_EPOCHS=0 SFT_BATCH_SIZE=4 GEN_BATCH_SIZE=32 LORA_R=512 SFT_LR=5e-5 N_LABELED=723 HIST_BINS=50 LOG_PERPLEXITY=1 N_PERPLEXITY=64 LOG_PPL_DIST=1 PPL_DIST_CAP=0 PPL_BATCH=$(pplbatch) SEED_BASE_DATA=1 PARSE_MODE=strict DO_SAMPLE=0 WANDB_RUN_SUFFIX=_section3_model_icl"

output            = /home/gsmithline/perfsim/experiments/condor/logs/$(tag).out
error             = /home/gsmithline/perfsim/experiments/condor/logs/$(tag).err
log               = /home/gsmithline/perfsim/experiments/condor/logs/$(tag).log

notification      = Complete
notify_user       = gabriel.smithline@tue.ellis.eu
on_exit_hold      = (ExitCode =!= 0)
periodic_release  = (NumJobStarts < 5) && ((time() - EnteredCurrentStatus) > 180)
periodic_remove   = (JobStatus == 5) && (NumJobStarts >= 5) && ((time() - EnteredCurrentStatus) > 600)

queue tag, style, beta, seed, deploy_every, regime, pscale, anchor, pop, eps, gamma, wplat, mode, canary, lam, kldir, sweeps, iclk, snap, uselora, fresh, ansk, gg, nrounds, basemodel, chatthink, mem, disk, pplbatch from experiments/condor/configs_pofd_section3_model_icl_greedy_smoke.txt
