# HTCondor: FIGURE-4 ANCHOR TRADE-OFF -- WHOLE-NODE PRODUCTION (pair b: g182, g184), 4 node job(s).
# GENERATED by gen_pofd_sweep.py from the F4A node-pack block. Never edit by hand.
# Each node job runs one manifest group (<= 8 cells, one per A100) through
# experiments/condor/run_node_pack.sh -> node_pack.py, which rebuilds every
# cell's env/args from its own single-GPU sub + configs row and runs the
# same idempotent wrapper: per-tag artifacts and logs are the ordinary ones.
# The request equals the node totals EXACTLY (the nodes' START policy is an
# equality on all four resources). Hold/release: a failed cell makes the
# node job exit non-zero; on release the wrapper skips completed cells.
universe          = vanilla
executable        = /home/gsmithline/perfsim/experiments/condor/run_node_pack.sh
arguments         = experiments/condor/nodepack_fig4_anchor_tradeoff.json $(group)

request_cpus      = 128
request_memory    = 2051934
request_disk      = 22345412368
request_gpus      = 8
requirements      = (TARGET.CUDADeviceName == "NVIDIA A100-SXM4-80GB") && (TARGET.TotalCpus == 128) && (TARGET.TotalMemory == 2051934) && (TARGET.TotalGpus == 8) && (TARGET.TotalDisk == 22345412368) && ((TARGET.Machine == "g182.internal.cluster.is.localnet") || (TARGET.Machine == "g184.internal.cluster.is.localnet"))

getenv            = False
environment       = "REPO=/home/gsmithline/perfsim CONDA_SH=/home/gsmithline/miniconda3/etc/profile.d/conda.sh ENV_NAME=opdyn"

output            = /home/gsmithline/perfsim/experiments/condor/logs/nodepack_fig4_anchor_tradeoff_node_b_$(group).out
error             = /home/gsmithline/perfsim/experiments/condor/logs/nodepack_fig4_anchor_tradeoff_node_b_$(group).err
log               = /home/gsmithline/perfsim/experiments/condor/logs/nodepack_fig4_anchor_tradeoff_node_b_$(group).log

notification      = Complete
notify_user       = gabriel.smithline@tue.ellis.eu
on_exit_hold      = (ExitCode =!= 0)
periodic_release  = (NumJobStarts < 5) && ((time() - EnteredCurrentStatus) > 180)
periodic_remove   = (JobStatus == 5) && (NumJobStarts >= 5) && ((time() - EnteredCurrentStatus) > 600)

queue group from experiments/condor/configs_pofd_fig4_anchor_tradeoff_node_b.txt
